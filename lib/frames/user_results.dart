import 'package:flutter/material.dart';

class UserResults extends StatelessWidget {
  const UserResults(UserModel, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
